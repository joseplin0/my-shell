# my-shell 🐚

这里是我的个人脚本库。

### Ubuntu/Debian 环境一键初始化
自己使用的初始化脚本，适配小鸡。
- 包含 `curl`, `wget`, `git`, `htop`, `vim`, `unzip`, `zip`, `jq`, `tmux`, `lsof`, `net-tools`, `Docker`, `Docker Compose`, `ZeroTier`, `cloudflared`
- 设置 `Asia/Shanghai`, `Swap 2-4GB`

#### 一键命令
```
curl -sL https://raw.githubusercontent.com/joseplin0/my-shell/main/server_init.sh | sudo bash
```

